#!/usr/bin/env node
/**
 * CLI entry point. Boots the local server, then opens the user's
 * default browser at the UI URL. Invoked via `npx relay-inspector`.
 *
 * Stub for now — to be filled in after the server has real behavior.
 */
import('./src/server/index.js');
