/**
 * Service worker. Routes inspector messages between content scripts
 * (one per inspected tab) and devtools panels (one per DevTools window
 * on each inspected tab).
 *
 * Stub for now — content/panel wiring lands in the next change.
 */
export {};
