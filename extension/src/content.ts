/**
 * Content script. Runs at document_start in the user's page (isolated
 * world) so it can inject our main-world bootstrap before any Relay
 * Environment is constructed, and bridge messages between the page and
 * the extension's service worker.
 *
 * Stub for now — wiring lands in the next change.
 */
export {};
