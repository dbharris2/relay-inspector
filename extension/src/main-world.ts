/**
 * Page-world bootstrap. Injected from the content script into the
 * inspected page's own JavaScript context so it can see
 * `__RELAY_DEVTOOLS_HOOK__` / Relay environments / etc. — content
 * scripts run in an isolated world and can't read those.
 *
 * Stub for now — hook installation lands in the next change.
 */
export {};
